[project]
name = "wordcli"
version = "0.1.0"
description = "CLI tool for inspecting and editing Word (.docx) documents"
requires-python = ">=3.9"

[project.scripts]
wordcli = "wordcli.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
